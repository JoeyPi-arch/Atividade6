# Atividade Avaliativa - Git Colaborativo com Portugol

## Integrantes do grupo:
João Pedro Batista de Jesus
Lucas Nisrallah Saab

## Objetivo
Desenvolver colaborativamente um algoritmo em Portugol de média de alunos

## João Pedro Batista de Jesus
Criou o repositório público e convidou Lucas Saab para ser colaborador de Atividade6
O repositorio foi criado com o readme.MD e depois foi adicionado o arquivo de portugol “sem título.por”
No arquivo do portugol estava a base do código para somar a média do aluno, pedindo 3 notas e fazendo média delas. O arquivo foi modificado para escrever se o aluno reprovou ou foi aprovado com comando “se” e “senao se”. Em seguida foi feito o git add, git commit e git push, atualizando o repositório.
O projeto foi finalizado com a atualização do reade.MD

### Foram utilizados os comandos:
git config --global user.name ” ”
git config --global  user.email “ ”
ssh-keygen -t rsa -b 4096 -C " "
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
clip < ~/.ssh/id_rsa.pub
ssh -T git@github.com
git clone git@github.com:JoeyPi-arch/Atividade6.git
cd repositório
git add .
git commit -m "Descrição da alteração"
git push

## Lucas Nisrallah Saab
Foi convidado para ser colaborador de João Batista, após configurar o git no chromebook, fez clone no repositório do github e modificou os arquivos, esta modificação foi que em vez da nota ser feita por média será feita por pesos, cada nota tendo um específico.
Em seguida foi feito o git add ., git commit e git push, isto gerou um conflito nos arquivos que gerou em um commit com apenas as modificações feita por ele, então foram os arquivos foram modificados para a versão certa e o código foi alterado para o último versao.
Ao finalizar o código, foi feito o git add ., git commit e git push finalizando as mudanças do código.

### Foram utilizados os comandos:
git config --global user.name ” ”
git config --global  user.email “ ”
ssh-keygen -t rsa -b 4096 -C " "
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
clip < ~/.ssh/id_rsa.pub
ssh -T git@github.com
git clone git@github.com:JoeyPi-arch/Atividade6.git
cd repositório
git pull
git add .
git commit -m "Descrição da alteração"
git push
